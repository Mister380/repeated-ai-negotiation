# Current research context

The active protocol compares Anthropic, OpenAI and intended open-source candidates as three core groups. Distinguish open weights from verified open-source licensing. The eight-candidate design is all 36 unordered pairs, five regimes, 24 trajectories per cell, balanced across four role/mover blocks: 4,320 independent runs and 14,688 episodes. Earlier anchor-only schedules are superseded. Candidate endpoints and settings are unverified until access checks.

For current submission prose, omit budget and fallback discussion. Retain existing execution safeguards and the internal open-source contingency for possible later use; do not activate or describe it as the primary design. No endpoint access, pilot, empirical finding, ethics approval or signature may be inferred from software checks.

Primary analysis is whole-trajectory D1 minus D0 over episodes 2–4, equal pairs and blocks. Never silently reweight absent cells or omit scheduled missing runs from bounds. The six group-pair summaries differ from causal provider effects. Unsupported interaction and single-shot p-values remain unavailable.

The synthetic calibration artifact records 90/100 coverage for nominal 95% small-cell percentile intervals. This is an unresolved pre-registration calibration limitation, not a successful coverage validation. Preserve the seed and result; extend simulations and compare justified procedures before real collection. Unit tests and mock runs do not validate real-model behavior.
